[project]
name = "telegram-cli-bridge"
version = "0.1.0"
description = "Remote CLI interaction via Telegram bot"
requires-python = ">=3.13"
dependencies = [
    "python-dotenv>=1.1.1",
    "python-telegram-bot>=22.5",
]

[project.scripts]
tg-cli-bridge = "main:main"
